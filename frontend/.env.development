# Применяется только при `npm run dev` (vite dev).
# Локальный backend (Spring Boot) работает на :8080.
# В production используется общий .env с пустым VITE_API_URL.
VITE_API_URL=http://localhost:8080
