## Plan: Изменение стартового виртуального капитала

### Целевые значения
| Константа | Старое | Новое |
|---|---|---|
| `GUEST_INITIAL_BALANCE` | `100000.0000` | `5000.0000` |
| `GUEST_REGISTRATION_BONUS` | `400000.0000` | `20000.0000` |
| `INITIAL_BALANCE` | `500000.0000` | `25000.0000` (= `GUEST_INITIAL_BALANCE + GUEST_REGISTRATION_BONUS`) |

### Backend — основной код (1 файл)

**`src/main/java/ImiTrade/user/domain/UserService.java`** (строки 30-37)
- Изменить `GUEST_INITIAL_BALANCE` → `"5000.0000"`
- Изменить `GUEST_REGISTRATION_BONUS` → `"20000.0000"`
- Изменить `INITIAL_BALANCE`: вместо `"500000.0000"` сделать **производное значение**, чтобы устранить дублирование магического числа:
  ```java
  public static final BigDecimal GUEST_INITIAL_BALANCE = new BigDecimal("5000.0000");
  public static final BigDecimal GUEST_REGISTRATION_BONUS = new BigDecimal("20000.0000");
  /** Direct registration parity: same final balance as guest→registered conversion. */
  public static final BigDecimal INITIAL_BALANCE = GUEST_INITIAL_BALANCE.add(GUEST_REGISTRATION_BONUS);
  ```
- Использование `INITIAL_BALANCE` в `register()` (строка 67) остаётся без изменений — значение вычисляется автоматически.

**Документация в Swagger-аннотациях (magic numbers в описаниях):**
- `src/main/java/ImiTrade/auth/web/AuthController.java:41` — `500000.00` → `25000.00`
- `src/main/java/ImiTrade/guest/web/GuestController.java:34` — `100000.00` → `5000.00`
- `src/main/java/ImiTrade/guest/dto/GuestResponse.java:14` — example `100000.0000` → `5000.0000`

### Backend — тесты

Тесты хардкодят значения литералами, ни один не ссылается на константы по имени. Значения, которые нужно обновить:

**Ключевые тесты бизнес-логики (меняются вместе с константами):**
- `src/test/java/ImiTrade/user/domain/UserServiceTest.java` — строки 43 (DisplayName), 66-67 (500000→25000), 144 (DisplayName 100000→5000), 156 (100000→5000), 190 (100000→5000 в фикстуре), 207 (500000→25000 = 5000+20000)
- `src/test/java/ImiTrade/auth/AuthIntegrationTest.java` — строки 68-69 (500000→25000), 81 (100000→5000), 104 (500000→25000)
- `src/test/java/ImiTrade/auth/domain/AuthServiceTest.java` — строки 153 (500000→25000), 162 (100000→5000)
- `src/test/java/ImiTrade/guest/domain/GuestServiceTest.java` — строки 28 (DisplayName), 34, 43 (100000→5000)
- `src/test/java/ImiTrade/guest/GuestIntegrationTest.java` — строки 47, 52, 56 (100000→5000)
- `src/test/java/ImiTrade/guest/GuestFlowIntegrationTest.java` — строки 62, 66 (100000→5000), 108 (комментарий с арифметикой: `100000 - 3105 + 400000 = 496895` → `5000 - 3105 + 20000 = 21895`), 112, 119 (`496895` → `21895`)

**Фикстурные балансы в тестах, не относящихся к бизнес-правилу** — проверяю индивидуально: там где баланс — просто «достаточно денег для сделки» (например, `TradeServiceTest` использует 500000/100000 как начальный баланс покупателя/продавца), значения **оставлю как есть** (это fixture data, не бизнес-инвариант; сделка на 3105 прошла бы и при новом балансе 25000). Граничные кейсы (`InsufficientBalance`) проверю отдельно и обновлю только если начальный баланс окажется меньше суммы сделки. Конкретно:
- `TradeServiceTest` (78, 95, 104, 121, 157, 184, 204, 211, 231, 250, 264) — балансы 500000/100000 как fixture, **не трогаю** (не assertions бизнес-правил)
- `PortfolioPositionRepositoryTest` (46, 50), `TransactionRepositoryTest` (54, 58) — балансы 500000 как fixture, **не трогаю**
- `AccountSecurityTest` (77, 79, 99, 105) — assertion баланса 500000 после регистрации. Здесь тестируется матрица доступа, а не бизнес-правило баланса; но значение берётся из реального `register()`, поэтому **обновлю 500000→25000** и производный `502151`→`27151` (25000+2151).
- `SecurityAccessTest` (101) — assertion guest balance `100000.0`. Берётся из реального `createGuest()`, поэтому **обновлю 100000→5000**.

### Frontend — только документация/комментарии

Отображаемых значений нет (`AccountSummary` берёт `data.balance` из API динамически). Обновить:
- `frontend/src/features/auth/model/useRegisterMutation.ts:22` — комментарий `+400 000` → `+20 000`
- `frontend/CLAUDE.md:170` — `+400 000` → `+20 000`
- `frontend/src/shared/utils/format.ts:5,16` — `100000` в примерах форматирования: **оставлю** (это демонстрация `formatMoney`, не бизнес-значение; замена на 25000 только запутает)

### Документация репозитория

- `readme.md` — обновить упоминания: строки 9-11 (100 000 → 5 000, 400 000 → 20 000), 17-18 (100 000 → 5 000, 400 000 → 20 000), 235-236 (+400 000 → +20 000), 456/461 (баланс в JSON-примерах 100000 → 5000), 490 (+400 000 → +20 000)
- `claude.md` — строки 287-290: обновить значения бизнес-правил (500000→25000, 100000→5000, 400000→20000), сохранить описание того, что `INITIAL_BALANCE` — это parity-значение

### Регрессионная проверка / качество

После изменений:
1. **Backend сборка + тесты:** `./gradlew test` — проверить, что все unit/integration/security тесты проходят, нет ошибок компиляции
2. **Frontend:** `cd frontend && npm run build && npm run lint` — проверить отсутствие ошибок (изменения только в комментариях, но сборку прогнать обязательно)

### Чего НЕ меняется
- Логика конвертации гостя (`convertGuestToRegistered`): бонус **начисляется** (`balance.add(BONUS)`), а не устанавливается — соответствует требованию ТЗ (сделки до регистрации сохраняются, бонус фиксированный)
- Перенос портфеля и истории операций под тем же `user_id`
- JWT-аутентификация, Guest Mode, торговля, security-цепочка

### Порядок выполнения
1. Backend: `UserService.java` (константы) → Swagger-описания (3 файла)
2. Backend-тесты: обновить assertions согласно списку выше
3. Frontend: 2 комментария в коде + CLAUDE.md
4. Документация: readme.md, claude.md
5. Запуск `./gradlew test`, затем `npm run build && npm run lint`; исправить всё, что упало